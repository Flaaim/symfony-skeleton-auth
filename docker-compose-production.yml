services:
  gateway:
    image: ${REGISTRY}/symfony-gateway:${IMAGE_TAG}
    restart: always
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt:ro
      - /var/www/html:/var/www/html:ro
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - frontend
      - api

  frontend:
    image: ${REGISTRY}/symfony-frontend:${IMAGE_TAG}
    restart: always

  api:
    image: ${REGISTRY}/symfony-api:${IMAGE_TAG}
    restart: always
    depends_on:
      - api-php-fpm

  api-php-fpm:
    image: ${REGISTRY}/symfony-api-php-fpm:${IMAGE_TAG}
    restart: always

  api-php-cli:
    image: ${REGISTRY}/symfony-api-php-cli:${IMAGE_TAG}
